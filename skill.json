{
  "name": "tapauth",
  "description": "Get delegated access for AI agents via TapAuth \u2014 the trust layer between humans and AI agents. Use when your agent needs OAuth access to GitHub, Google Workspace, Gmail, Linear, or another provider, or when it needs a user-supplied password or fixed API key. One API call creates an approval request; the user approves in browser; the agent retrieves the approved token or secret.",
  "version": "1.0.4"
}
